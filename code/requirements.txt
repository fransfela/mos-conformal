# requirements.txt — ICASSP 2027 experiment dependencies
# Python 3.10+
#
# Install: pip install -r requirements.txt
#
# GPU note: for NISQA and UTMOS, install the CUDA-enabled PyTorch first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# then run: pip install -r requirements.txt --ignore-installed torch torchvision torchaudio

# Core numerical / scientific
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3

# Audio I/O and processing
soundfile>=0.12
librosa>=0.10

# Deep learning (CPU fallback; replace with CUDA build for GPU)
torch>=2.0
torchaudio>=2.0

# DNSMOS ONNX runtime
onnxruntime>=1.16       # CPU; use onnxruntime-gpu for GPU
onnx>=1.14              # for ONNX graph manipulation in extract_features.py

# Plotting
matplotlib>=3.7

# Utilities
tqdm>=4.65
